# Phase 2 runtime dependencies (Python 3.10+ recommended)
numpy>=1.24,<3.0
laspy>=2.5,<3.0
matplotlib>=3.8,<4.0
pillow>=10.0,<12.0

# Optional:
# laszip>=0.2.3       # only needed if reading .laz files
